services:
  modmail:
    build: .
    container_name: modmail
    restart: unless-stopped
    init: true
    env_file:
      - .env
    environment:
      DB_PATH: /data/modmail.sqlite
    volumes:
      - ./data:/data
    stop_grace_period: 20s
